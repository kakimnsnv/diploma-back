version: "3.8"

services:
#  app:
#    image: your_dockerhub_username/medical-app:latest
#    container_name: medical_imaging_app
#    ports:
#      - "8080:8080"
#    environment:
#      - DB_HOST=postgres
#      - DB_PORT=5432
#      - DB_USER=postgres
#      - DB_PASSWORD=password
#      - DB_NAME=diploma_db
#      - PORT=8080
#      - JWT_SECRET=change-this-secret-for-dev
#      - MINIO_ENDPOINT=localhost:9000
#      - MINIO_ACCESS_KEY=minioadmin
#      - MINIO_SECRET_KEY=minioadmin
#      - MINIO_BUCKET=medical-imaging
#      - MINIO_USE_SSL=false
#    depends_on:
#      postgres:
#        condition: service_healthy
#      minio:
#        condition: service_healthy

  postgres:
    image: postgres:15-alpine
    container_name: medical_imaging_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: diploma_db
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: medical_imaging_minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  postgres_data:
  minio_data: